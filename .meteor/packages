# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.4.0             # Packages every Meteor app needs to have
mobile-experience@1.0.5       # Packages for a great mobile UX
mongo@1.6.0                   # The database Meteor supports right now
standard-minifier-css@1.5.0   # CSS minifier run for production mode
standard-minifier-js@2.4.0    # JS minifier run for production mode
shell-server@0.4.0            # Server-side component of the `meteor shell` command
ecmascript@0.12.0             # Enable ECMAScript2015+ syntax in app code
dynamic-import                # Allows to conditionally import modules on runtime

static-html                   # This build plugin parses all of the .html files in your app (needed to hook React top node into the DOM)

tracker                       # Meteor's client-side reactive programming library
reactive-var@1.0.11           # Reactive variable for tracker
react-meteor-data             # Connects Meteor Tracker layer to React

audit-argument-checks         # This package causes Meteor to require that all arguments passed to methods and publish functions are typechecked
#appcache                     # Makes app work offline by storing the static parts of a Meteor application (the client side Javascript, HTML, CSS, and images) in the browser’s application cache

autopublish@1.0.7             # Publish all data to the clients (for prototyping)
insecure@1.0.7                # Allow all DB writes from clients (for prototyping)

jquery                        # For Semantic ui